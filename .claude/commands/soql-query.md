Build an optimized SOQL query for this requirement: $ARGUMENTS

- Check existing custom objects and fields in the project to use correct API names
- Include field-level security considerations
- Add comments explaining any filters or relationships
- Flag if any fields use AI_* or Coder_* prefixes
- Suggest indexes if querying large data volumes