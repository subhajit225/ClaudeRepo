@isTest
public class OpportunityServiceTest {
    /*
static testMethod void OpportunityServiceTestMethod() {
    
//Created record from scratch
    // Create a test account
     Account testAcct = new Account (Name = 'My Test Account');
     insert testAcct;
    // Create a test account
     Contact testCon = new Contact (LeadSource ='Channel',LastName = 'My Test Contact',AccountId =testAcct.Id ,Email='test123@test.com',FirstName='Shadab');
     insert testCon;
     
     TriggerControl__c setting = new TriggerControl__c();
    setting.Name = 'Opportunity';
    setting.DisableTrigger__c = False;
    insert setting;
    // Creates first opportunity
    
    Opportunity oppt = new Opportunity(Name ='New mAWS Deal',
                            AccountID = testAcct.ID,
                            StageName = '1 Discovery',
                            Amount = 3000,
                            CloseDate = System.today()+30,
                            Type = 'New Customer',
                            Lead_Email_Hidden__c ='test123@test.com',
                            Alliance_Partner_Name__c='Amazon', Alliance_Source_Type__c='Alliance Initiated Deal'
                            );
    
    insert oppt;
    }*/
}