<apex:page standardController="Case" rendered="{!$User.UITheme == 'Theme3'}">
    <apex:slds />
    <apex:includeLightning />
    <apex:includeScript value="/support/console/42.0/integration.js"/>
    <div id="lightning" /> 
    
    <script type="text/javascript">
    if(sforce.console.isInConsole()) {
        
    }
    $Lightning.use("c:fedBanner", function() {
        $Lightning.createComponent(
            "c:CaseEntitlement",
            {"recordId" : "{!Case.Id}"},
            "lightning",
            function(cmp) {
                // do some stuff
            });
        $Lightning.createComponent(
            "c:L3SupportBannerCase_Lightning",
            {"recordId" : "{!Case.Id}"},
            "lightning",
            function(cmp) {
                // do some stuff
            });
    });
    </script>
</apex:page>